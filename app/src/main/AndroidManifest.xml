<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yuan.dyhdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.ListActivity"/>
        <activity android:name=".activity.RcyActivity"/>
        <activity android:name=".activity.KotlinActivity"/>
        <activity android:name=".activity.MaterialDesign"/>
        <activity android:name=".activity.JetpackAct"/>
        <activity android:name=".activity.ZDYViewActivity"/>
        <activity android:name=".activity.ScrollviewActivity"
            android:theme="@style/AppTheme"/>
        <service android:name=".service.MyService"
            android:enabled="true"
            android:exported="false"/>
    </application>

</manifest>